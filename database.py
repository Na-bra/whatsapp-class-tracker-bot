"""
database.py — placeholder

This file intentionally contains no executable code. Implement the JSON
read/write helpers (`load_data`, `save_data`, etc.) when ready. Keep this
module as the single place to access `classes.json`.
"""

# No code present by request. Add implementation later.
